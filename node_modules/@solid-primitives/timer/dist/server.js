import {
  makeTimer
} from "./chunk-EYUPG57E.js";

// src/server.ts
var makeTimer2 = makeTimer;
var createTimer = () => {
};
var createTimeoutLoop = () => {
};
var createPolled = (fn, timeout, prev) => {
  const value = fn(prev);
  return () => value;
};
var createIntervalCounter = () => () => 0;
export {
  createIntervalCounter,
  createPolled,
  createTimeoutLoop,
  createTimer,
  makeTimer2 as makeTimer
};
