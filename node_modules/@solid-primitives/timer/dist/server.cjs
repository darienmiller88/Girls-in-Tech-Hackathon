"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/server.ts
var server_exports = {};
__export(server_exports, {
  createIntervalCounter: () => createIntervalCounter,
  createPolled: () => createPolled,
  createTimeoutLoop: () => createTimeoutLoop,
  createTimer: () => createTimer,
  makeTimer: () => makeTimer2
});
module.exports = __toCommonJS(server_exports);

// src/index.ts
var import_solid_js = require("solid-js");
var makeTimer = (fn, delay, timer) => {
  const intervalId = timer(fn, delay);
  const clear = () => clearInterval(intervalId);
  return (0, import_solid_js.onCleanup)(clear);
};

// src/server.ts
var makeTimer2 = makeTimer;
var createTimer = () => {
};
var createTimeoutLoop = () => {
};
var createPolled = (fn, timeout, prev) => {
  const value = fn(prev);
  return () => value;
};
var createIntervalCounter = () => () => 0;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createIntervalCounter,
  createPolled,
  createTimeoutLoop,
  createTimer,
  makeTimer
});
